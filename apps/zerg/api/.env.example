# Database connection string
DATABASE_URL=postgres://postgres:postgres@localhost:5432/zerg_api

# Server configuration
HOST=0.0.0.0
PORT=3000

# Logging configuration
# RUST_ENV=development     # Use 'production' for JSON logs, 'development' for pretty logs
# RUST_LOG=info           # Override log levels (optional)
